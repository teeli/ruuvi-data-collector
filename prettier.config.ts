import type { Config } from 'prettier'

const config: Config = {
  objectWrap: 'collapse',
  printWidth: 120,
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'es5',
}

/* oxlint-disable no-default-export */
export default config
